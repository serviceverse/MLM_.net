@{
    ViewData["Title"] = "Withdraw";
}

<div class="min-h-screen bg-primary text-[var(--text-primary)] p-8">
    <div class="max-w-6xl mx-auto space-y-8">
        <!-- Header -->
        <div>
            <h1 class="text-4xl font-bold text-[var(--text-primary)] mb-2">Withdraw</h1>
            <p class="text-[var(--text-secondary)]">
                Withdraw funds from your wallet or trading accounts securely
            </p>
        </div>

        <div class="bg-[var(--bg-secondary)] border border-[var(--border-primary)] rounded-3xl p-8 text-center text-[var(--text-tertiary)]">
            <h3 class="text-2xl font-bold mb-4">Withdrawal Options</h3>
            <p>Withdraw via Crypto (USDT) or Bank Transfer. Form implementation will follow in next iterations.</p>
        </div>
    </div>
</div>
