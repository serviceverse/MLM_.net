@{
    ViewData["Title"] = "List";
}

<div class="min-h-screen bg-primary text-[var(--text-primary)] p-8">
    <div class="max-w-6xl mx-auto space-y-8">
        <div>
            <h1 class="text-4xl font-bold text-[var(--text-primary)] mb-2">List</h1>
            <p class="text-[var(--text-secondary)]">Migrated from: (admin)\user\list\page.tsx</p>
        </div>
        <div class="bg-[var(--bg-secondary)] border border-[var(--border-primary)] rounded-3xl p-8">
            <p class="text-[var(--text-tertiary)]">Content for List will go here.</p>
        </div>
    </div>
</div>

