@{
    ViewData["Title"] = "Success";
}

<div class="p-6 md:p-8">
    <div class="max-w-6xl space-y-4">
        <div>
            <h1 class="text-2xl font-bold text-[var(--text-primary)]">Success</h1>
            <p class="text-[var(--text-secondary)]">Migrated from: payment\success\page.tsx</p>
        </div>
        <div class="flex-1 overflow-hidden flex flex-col">
            <p class="text-[var(--text-tertiary)]">Content for Success will go here.</p>
        </div>
    </div>
</div>
